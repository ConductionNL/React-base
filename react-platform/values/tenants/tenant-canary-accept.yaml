---
# Canary tenant — accept environment.
# Wave 0 zodat hij vóór alle andere accept-tenants synct.
# Geen image-tag override — gebruikt de pin in common.yaml
# (development-V1.0.260422), test daarmee de huidige kandidaat-versie.
tenant:
  name: canary
  environment: accept
  wave: "0"
